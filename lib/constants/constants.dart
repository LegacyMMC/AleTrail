import 'dart:ui';

import 'package:flutter/material.dart';

//  TEXT COLOURS
const Color mainText = Color.fromARGB(0, 0, 0, 0);
const Color secondaryText = Color.fromARGB(0, 0, 0, 0);

// BUTTON COLOURS
const Color primaryButton = Color(0xFFF88601);
const Color secondaryButton =  Color(0xFF002128);